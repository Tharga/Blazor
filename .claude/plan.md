# Plan: optional-scope-registries

## Steps

1. [ ] Update `TeamComponent.razor` — replace `@inject` with optional resolution via `IServiceProvider`
2. [ ] Update `ApiKeyView.razor` — same approach for `IScopeRegistry`
3. [ ] Write/update tests
4. [ ] Build and test
5. [ ] Commit
